<clickhouse>
    <listen_host>0.0.0.0</listen_host>

    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <interserver_http_port>9009</interserver_http_port>

    <!-- Replication macros -->
    <!-- HA with 2 nodes => 1 shard, 2 replicas -->
    <macros>
        <cluster>logcelot_cluster</cluster>
        <shard>01</shard>
        <replica>clickhouse-01</replica>
    </macros>

    <!-- ClickHouse Keeper (ZK-compatible) -->
    <zookeeper>
        <node><host>keeper-01</host><port>9181</port></node>
        <node><host>keeper-02</host><port>9181</port></node>
        <node><host>keeper-03</host><port>9181</port></node>
        <session_timeout_ms>30000</session_timeout_ms>
        <operation_timeout_ms>10000</operation_timeout_ms>
    </zookeeper>

    <!-- Cluster configuration for Distributed tables -->
    <!-- 1 shard with 2 replicas -->
    <remote_servers>
        <logcelot_cluster>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>clickhouse-01</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>clickhouse-02</host>
                    <port>9000</port>
                </replica>
            </shard>
        </logcelot_cluster>
    </remote_servers>

    <!-- Performance -->
    <max_connections>4096</max_connections>
    <keep_alive_timeout>3</keep_alive_timeout>
    <max_concurrent_queries>100</max_concurrent_queries>
    <uncompressed_cache_size>8589934592</uncompressed_cache_size>
    <mark_cache_size>5368709120</mark_cache_size>

    <!-- Logging -->
    <logger>
        <level>information</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <size>100M</size>
        <count>10</count>
    </logger>

    <!-- Compression -->
    <compression>
        <case>
            <min_part_size>10485760</min_part_size>
            <min_part_size_ratio>0.01</min_part_size_ratio>
            <method>zstd</method>
            <level>3</level>
        </case>
    </compression>
</clickhouse>
